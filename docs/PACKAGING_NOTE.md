# Packaging Note

This folder was assembled to be GitHub-upload ready.

Included:
- simulation source code (`src/`)
- manuscript (`paper/`)
- selected experiment outputs (`results/`)

If you re-run experiments, new outputs may be generated in repository root as `results_*`.
